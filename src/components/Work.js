import React, {Component} from 'react';
import Scroll from 'react-scroll';

//styles
import './work.scss';

const scroll = Scroll.animateScroll;
const Link   = Scroll.Link;

class Work extends Component {
  render() {
    return (
      <Element className="work-section" name="work" id="work">
			</Element>
    )
  }
}

export default Work;
